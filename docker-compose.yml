version: "3.9"
services:
  app:
    build: .
    image: medios-get:latest
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - PORT=8000
      - HOST=0.0.0.0
      - FLASK_DEBUG=0
    ports:
      - "8000:8000"
    restart: unless-stopped
